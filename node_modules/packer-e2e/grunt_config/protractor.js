var grunt = require('grunt');

module.exports = {
  protractor: {
    options: {
      keepAlive: true, // If false, the grunt process stops when the test fails.
      noColor: false, // If true, protractor will not use colors in its output.
      args: {
        // Arguments passed to the command
      }
    },
    chrome: {
      options: {
        configFile: grunt.file.exists('test/e2e.conf.js') ? 'test/e2e.conf.js' : 'node_modules/packer-e2e/config/e2e.config.js',
      }
    },
    phantomjs: {
      options: {
        configFile: grunt.file.exists('test/e2e-phantom.conf.js') ? 'test/e2e-phantom.conf.js' : 'node_modules/packer-e2e/config/e2e-phantom.config.js',
      }
    }
  }
};
